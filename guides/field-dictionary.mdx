---
title: 'Field dictionary'
description: 'Reference for common ID fields, data types, timestamp formats, and shared enums used across Revive API endpoints — deals, users, contacts, and reports.'
---

## Common ID fields

These ID fields appear across many endpoints and follow consistent patterns.

| Field | Type | Format | Example | Description |
|-------|------|--------|---------|-------------|
| `uid` | string | Firebase UID | `"user_abc123"` | User identifier from Firebase Auth |
| `dealId` | string | Internal ID | `"deal_xyz789"` | Deal/project identifier |
| `opportunityId` | string | Internal ID | `"opp_abc123"` | Sales opportunity identifier |
| `contactId` | string | Internal ID | `"cont_abc123"` | Contact record identifier |
| `contractorId` | string | Internal ID | `"contr_abc123"` | Contractor record identifier |
| `companyId` | string | Internal ID | `"comp_abc123"` | Brokerage/company identifier |
| `reportId` | string | Internal ID | `"rpt_xyz789"` | Property or Vision AI report identifier |
| `estimateId` | string | Internal ID | `"est_abc123"` | Renovation estimate identifier |
| `rmaId` | string | Internal ID | `"rma_001"` | Renovation Management Agreement identifier |
| `contractId` | string | Internal ID | `"contract_abc123"` | E-signature contract identifier |
| `hubspotOwnerId` | string | HubSpot ID | `"owner_abc123"` | HubSpot CRM owner identifier |

---

## Timestamps

All timestamps are ISO 8601 UTC unless noted otherwise.

| Field | Format | Example |
|-------|--------|---------|
| `createdAt` | ISO 8601 | `"2026-04-15T18:00:00.000Z"` |
| `updatedAt` | ISO 8601 | `"2026-04-15T18:00:00.000Z"` |
| `sentAt` | ISO 8601 | `"2026-04-15T18:00:00.000Z"` |
| `completedAt` | ISO 8601 | `"2026-04-15T18:00:00.000Z"` |
| `signedAt` | ISO 8601 | `"2026-02-15T14:00:00.000Z"` |
| `executed_at` | ISO 8601 | `"2026-04-18T22:15:00.000Z"` |
| `startTime` | Unix ms | `1713100000000` |

<Info>
  `executed_at` is set on an RMA when it transitions to the `executed` status. The Admin RMA list uses this timestamp (falling back to `updated_at`) to decide whether an RMA falls into **Ready for Check Run** or **Next Check Run**, based on the most recent Friday 23:59:59 cutoff.
</Info>

<Warning>
  Some older endpoints use Unix milliseconds (`startTime`, `startDate`) instead of ISO 8601. Check the endpoint reference for the specific format.
</Warning>

---

## Status enums

### Deal type (`dealType` / `dealtype`)

On RMA responses from `useRmas` (Admin), `opportunity.dealtype` is returned as the human-readable **label** (right column). Other surfaces still expose the raw HubSpot ID.

| HubSpot ID | Label |
|------------|-------|
| `Contract Buy-out` | Renovate to sell |
| `Buy It Now` | Sell 360 |
| `Flip 360` | Flip 360 |
| `Buy with cash` | Buy with cash |
| `Prepare for sell` | Prepare to sell |
| `Bridge Financing` | Prepare to buy |
| `Trade Up Downleg` | Prepare to sell |
| `Special Services` | Renovate to stay |
| `TBD` | TBD |
| `Unknown` | Unknown |
| `Other` | Other |
| `B2B` | B2B |
| `Realtor Referral` | Realtor Referral |
| `Revive Lite` | Revive Lite |
| `Cash Offer` | Cash Offer |

### User role (`role`)

| Value | Description |
|-------|-------------|
| `admin` | Full platform access, manage all deals and users |
| `agent` | Real estate agent — view assigned deals and contacts |
| `contractor` | Renovation contractor — view assigned projects |
| `homeowner` | Property owner — view own reports and documents |

### Referral type (`referralType`)

| Value | Description |
|-------|-------------|
| `agent` | Referred by a real estate agent |
| `partner` | Referred by a Revive partner |
| `website` | Inbound from Revive website |
| `direct` | Direct contact / outbound |

### Report type (`reportType`)

| Value | Description |
|-------|-------------|
| `standard` | Standard property analysis report |
| `premium` | Enhanced report with additional comparables and detail |

### Application source (`application` / `source`)

| Value | Description |
|-------|-------------|
| `dashboard` | Created from the Revive Dashboard |
| `admin` | Created from the Admin panel |
| `intake` | Created from the intake/onboarding flow |
| `mobile` | Created from the mobile app |

### Estimate status

| Value | Description |
|-------|-------------|
| `draft` | Estimate is being prepared |
| `pending_signature` | Sent for homeowner signature |
| `approved` | Homeowner approved the estimate |
| `completed` | Work is done, estimate finalized |

### RMA status

| Value | Description |
|-------|-------------|
| `preparing` | RMA being drafted (was `drafting` before v2.150.0) |
| `pending_signatures` | Sent for signatures (was `sent`) |
| `executed` | All parties signed; `executed_at` is set (was `review`) |
| `not_approved` | Rejected (was `rejected`) |
| `paid` | Funds released (was `released`) |
| `hold` | Temporarily on hold |

### RMA rush status (`rush_status`)

Set when an RMA has been flagged as rush. Drives the rush request workflow on `/v1/rma/{rmaId}/rush`, `/rush/approve`, `/rush/decline`, and `/rush/remove`.

| Value | Description |
|-------|-------------|
| `pending` | Rush has been requested and is awaiting an admin decision |
| `approved` | Admin approved the rush request; `is_rush` is `true` |
| `declined` | Admin declined the rush request; `rush_reason` captures the decline reason |

Related fields on the RMA object:

| Field | Type | Description |
|-------|------|-------------|
| `is_rush` | boolean | `true` once a rush request has been approved |
| `rush_reason` | string | Reason supplied with the rush request or decline |
| `rush_requested_by` | object | Admin or user who submitted the rush request |
| `rush_requested_at` | ISO 8601 | Timestamp of the rush request |

### RMA activity actions (`RmaActivityLog.action`)

Each entry in an RMA's activity log uses one of the following action values.

| Value | Description |
|-------|-------------|
| `submitted` | RMA was created and submitted |
| `received` | RMA was received and acknowledged |
| `approved` | RMA was approved |
| `rejected` | RMA was rejected |
| `payment_released` | A payment was released against the RMA |
| `note_created` | An internal note was added |
| `note_deleted` | An internal note was removed |
| `note_updated` | An internal note was edited |
| `hold_status_changed` | Hold flag was toggled on the RMA |
| `approved_amount_updated` | Approved amount changed |
| `material_advance_updated` | Material advance amount changed |
| `final_payment_updated` | Final payment amount changed |
| `rush_requested` | Rush processing was requested |
| `rush_approved` | Admin approved the rush request |
| `rush_declined` | Admin declined the rush request |
| `rush_removed` | Rush flag was removed from the RMA |

### Onsite visit status (`sessionStatus`)

| Value | Description |
|-------|-------------|
| `scheduled` | Visit is booked |
| `in_progress` | Visit currently happening |
| `completed` | Visit finished, notes recorded |
| `cancelled` | Visit was cancelled |

---

## Property & valuation fields

These fields appear in property reports, Vision AI, and analysis endpoints.

| Field | Type | Example | Description |
|-------|------|---------|-------------|
| `streetAddress` | string | `"123 Main St, Los Angeles, CA 90001"` | Full street address |
| `state` | string | `"CA"` | Two-letter state code |
| `city` | string | `"Los Angeles"` | City name |
| `postalCode` | string | `"90001"` | ZIP code |
| `bedrooms` | number | `3` | Number of bedrooms |
| `bathrooms` | number | `2` | Number of bathrooms |
| `sqft` | number | `1850` | Square footage |
| `yearBuilt` | number | `1985` | Year the property was built |
| `estimatedValue` | number | `725000` | Current estimated property value (USD) |
| `estimatedAfterValue` | number | `875000` | Estimated value after renovation (USD) |
| `renovationBudget` | number | `45000` | Total renovation cost estimate (USD) |
| `roi` | number | `3.3` | Return on investment ratio |
| `timeline` | number | `6` | Estimated renovation timeline in months |
| `conditionScore` | number | `72` | AI-assessed property condition (0–100) |
| `conditionGrade` | string | `"C+"` | Letter grade from condition score |

---

## Pagination fields

| Field | Type | Description |
|-------|------|-------------|
| `total` | number | Total number of matching records |
| `data` | array | Array of result objects for the current page |
| `after` | string | Cursor value for the next page (cursor pagination) |

---

## Response envelope

Most endpoints return a consistent envelope:

```json
{
  "status": "success",
  "data": { ... },
  "message": "Optional human-readable message."
}
```

For errors:

```json
{
  "status": "error",
  "message": "What went wrong.",
  "errors": [{ "field": "email", "message": "Email is required." }]
}
```

See [Error handling](/guides/error-handling) for full details.
